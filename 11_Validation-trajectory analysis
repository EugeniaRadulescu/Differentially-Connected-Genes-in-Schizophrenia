
##### Analyze the 4 sets of organoids 04092026 ###########


rm(list = ls())  # Clear the environment
options(warn=-1) # Turn off warning message globally
library(monocle3) # Load Monocle

library(Seurat)
library(SeuratWrappers)
library(monocle3)

load("~/Your_directory/Organoids_01202026.RData")

cds_hOLs <- reduce_dimension(cds_hOLs,reduction_method = "UMAP")
p3<-plot_cells(cds_hOLs, label_groups_by_cluster=FALSE,  color_cells_by = "cell_type2",group_label_size = 5)

cds_COs <- reduce_dimension(cds_COs,reduction_method = "UMAP")
p4<-plot_cells(cds_COs, label_groups_by_cluster=FALSE,  color_cells_by = "cell_type2",group_label_size = 5)

p3+p4

cds_vso <- reduce_dimension(cds_vso,reduction_method = "UMAP")
p5<-plot_cells(cds_vso, label_groups_by_cluster=FALSE,  color_cells_by = "cell_type2",group_label_size = 5)

cds_hippo <- reduce_dimension(cds_hippo,reduction_method = "UMAP")
p6<-plot_cells(cds_hippo, label_groups_by_cluster=FALSE,  color_cells_by = "cell_type2",group_label_size = 5)

p5 + p6

cds_hOLs <- reduce_dimension(cds_hOLs)
cds_COs <- reduce_dimension(cds_COs)

cds_vso <- reduce_dimension(cds_vso)
cds_hippo <- reduce_dimension(cds_hippo)

#plot_cells(cds_COs, color_cells_by="Dx", label_cell_groups=FALSE,cell_size=0.45,cell_stroke=0.3)

cds_hOLs <- cluster_cells(cds_hOLs, resolution=1e-1)
p7<-plot_cells(cds_hOLs,group_label_size = 5,cell_size=0.6,cell_stroke=0.2)

cds_COs <- cluster_cells(cds_COs, resolution=1e-5)
p8<-plot_cells(cds_COs,group_label_size = 5,cell_size=0.45,cell_stroke=0.2)

p7 + p8

cds_vso <- cluster_cells(cds_vso, resolution=1e-5)
p9<-plot_cells(cds_vso,group_label_size = 5,cell_size=0.6,cell_stroke=0.2)

cds_hippo <- cluster_cells(cds_hippo, resolution=1e-5)
p10<-plot_cells(cds_hippo,group_label_size = 5,cell_size=0.45,cell_stroke=0.2)

p9+p10

plot_cells(cds_hOLs, color_cells_by="partition", group_cells_by="partition",group_label_size = 5)
plot_cells(cds_COs, color_cells_by="partition", group_cells_by="partition",group_label_size = 5)

p11<-plot_cells(cds_hOLs, color_cells_by="cell_type2",label_groups_by_cluster=FALSE,group_label_size = 5)
p12<-plot_cells(cds_COs, color_cells_by="cell_type2",label_groups_by_cluster=FALSE,group_label_size = 5)
p11+p12

p13<-plot_cells(cds_vso, color_cells_by="cell_type2",label_groups_by_cluster=FALSE,group_label_size = 5)
p14<-plot_cells(cds_hippo, color_cells_by="cell_type2",label_groups_by_cluster=FALSE,group_label_size = 5)
p13+p14

cds_hOLs <- learn_graph(cds_hOLs) # Save the cds_hOLs with learned graph
cds_COs <- learn_graph(cds_COs) # Save the cds_COs with learned graph
cds_vso <- learn_graph(cds_vso) # Save the cds_vso with learned graph
cds_hippo <- learn_graph(cds_hippo) # Save the cds_hippo with learned graph


# cds_hOLs<-order_cells(cds_hOLs)

table(pData(cds_hOLs)$cell_type2)
table(pData(cds_COs)$cell_type2)
table(pData(cds_vso)$cell_type2)
table(pData(cds_hippo)$cell_type2)


# Subset your organoids by cell-types:


neurons_vso <- which(colData(cds_vso)$cell_type2 %in% c("neurons"))
fetal_Q_vso <- which(colData(cds_vso)$cell_type2 %in% c("fetal_quiescent"))
repl_vso<- which(colData(cds_vso)$cell_type2 %in% c("fetal_replicating"))
astro_vso <- which(colData(cds_vso)$cell_type2 %in% c("astrocytes"))

neurons_COs <- which(colData(cds_COs)$cell_type2 %in% c("neurons")) # You will not use these
fetal_Q_COs <- which(colData(cds_COs)$cell_type2 %in% c("fetal_quiescent"))
repl_COs<- which(colData(cds_COs)$cell_type2 %in% c("fetal_replicating"))
astro_COs <- which(colData(cds_COs)$cell_type2 %in% c("astrocytes"))

oligo_OPC_hOL <- which(colData(cds_hOLs)$cell_type2 %in% c("oligodendrocytes", "OPC"))


cds_neurons_vso <- new_cell_data_set(
  expression_data = counts(cds_vso)[, neurons_vso], 
  cell_metadata = colData(cds_vso)[neurons_vso, ], 
  gene_metadata = rowData(cds_vso)
)

cds_fetal_Q_vso <- new_cell_data_set(
  expression_data = counts(cds_vso)[, fetal_Q_vso], 
  cell_metadata = colData(cds_vso)[fetal_Q_vso, ], 
  gene_metadata = rowData(cds_vso)
)

cds_repl_vso <- new_cell_data_set(
  expression_data = counts(cds_vso)[, repl_vso], 
  cell_metadata = colData(cds_vso)[repl_vso, ], 
  gene_metadata = rowData(cds_vso)
)

cds_astro_vso <- new_cell_data_set(
  expression_data = counts(cds_vso)[, astro_vso], 
  cell_metadata = colData(cds_vso)[astro_vso, ], 
  gene_metadata = rowData(cds_vso)
)



cds_neurons_COs <- new_cell_data_set(
  expression_data = counts(cds_COs)[, neurons_COs], 
  cell_metadata = colData(cds_COs)[neurons_COs, ], 
  gene_metadata = rowData(cds_COs)
)

cds_fetal_Q_COs <- new_cell_data_set(
  expression_data = counts(cds_COs)[, fetal_Q_COs], 
  cell_metadata = colData(cds_COs)[fetal_Q_COs, ], 
  gene_metadata = rowData(cds_COs)
)

cds_repl_COs <- new_cell_data_set(
  expression_data = counts(cds_COs)[, repl_COs], 
  cell_metadata = colData(cds_COs)[repl_COs, ], 
  gene_metadata = rowData(cds_COs)
)

cds_astro_COs <- new_cell_data_set(
  expression_data = counts(cds_COs)[, astro_COs], 
  cell_metadata = colData(cds_COs)[astro_COs, ], 
  gene_metadata = rowData(cds_COs)
)


cds_oligo_OPC_hOL <- new_cell_data_set(
  expression_data = counts(cds_hOLs)[, oligo_OPC_hOL], 
  cell_metadata = colData(cds_hOLs)[oligo_OPC_hOL, ], 
  gene_metadata = rowData(cds_hOLs)
)


# 3. Transfer the UMAP coordinates

reducedDims(cds_neurons_vso)$UMAP <- reducedDims(cds_vso)$UMAP[neurons_vso, ]
reducedDims(cds_fetal_Q_vso)$UMAP <- reducedDims(cds_vso)$UMAP[fetal_Q_vso, ]
reducedDims(cds_repl_vso)$UMAP <- reducedDims(cds_vso)$UMAP[repl_vso, ]
reducedDims(cds_astro_vso)$UMAP <- reducedDims(cds_vso)$UMAP[astro_vso, ]

reducedDims(cds_neurons_COs)$UMAP <- reducedDims(cds_COs)$UMAP[neurons_COs, ]
reducedDims(cds_fetal_Q_COs)$UMAP <- reducedDims(cds_COs)$UMAP[fetal_Q_COs, ]
reducedDims(cds_repl_COs)$UMAP <- reducedDims(cds_COs)$UMAP[repl_COs, ]
reducedDims(cds_astro_COs)$UMAP <- reducedDims(cds_COs)$UMAP[astro_COs, ]

reducedDims(cds_oligo_OPC_hOL)$UMAP <- reducedDims(cds_hOLs)$UMAP[oligo_OPC_hOL, ]


# 4. Re-Cluster & Learn Graph

cds_neurons_vso <- cluster_cells(cds_neurons_vso, reduction_method = "UMAP",k=5)
cds_neurons_vso <- learn_graph(cds_neurons_vso, use_partition = FALSE)

cds_fetal_Q_vso <- cluster_cells(cds_fetal_Q_vso, reduction_method = "UMAP",k=5)
cds_fetal_Q_vso <- learn_graph(cds_fetal_Q_vso, use_partition = FALSE)

cds_repl_vso <- cluster_cells(cds_repl_vso, reduction_method = "UMAP",k=5)
cds_repl_vso <- learn_graph(cds_repl_vso, use_partition = FALSE)

cds_astro_vso <- cluster_cells(cds_astro_vso, reduction_method = "UMAP",k=5)
cds_astro_vso <- learn_graph(cds_astro_vso, use_partition = FALSE)

cds_neurons_COs <- cluster_cells(cds_neurons_COs, reduction_method = "UMAP",k=5)
cds_neurons_COs <- learn_graph(cds_neurons_COs, use_partition = FALSE)

cds_fetal_Q_COs <- cluster_cells(cds_fetal_Q_COs, reduction_method = "UMAP",k=5)
cds_fetal_Q_COs <- learn_graph(cds_fetal_Q_COs, use_partition = FALSE)

cds_repl_COs <- cluster_cells(cds_repl_COs, reduction_method = "UMAP",k=5)
cds_repl_COs <- learn_graph(cds_repl_COs, use_partition = FALSE)

cds_astro_COs <- cluster_cells(cds_astro_COs, reduction_method = "UMAP",k=5)
cds_astro_COs <- learn_graph(cds_astro_COs, use_partition = FALSE)

cds_oligo_OPC_hOL <- cluster_cells(cds_oligo_OPC_hOL, reduction_method = "UMAP",k=5)
cds_oligo_OPC_hOL <- learn_graph(cds_oligo_OPC_hOL, use_partition = FALSE)


# 5. Visualize to confirm it's a clean 'backbone'


p_neurons_vso<-plot_cells(cds_neurons_vso, 
                     color_cells_by = "cell_type2", 
                     cell_size = 0.8,
                     label_principal_points = TRUE,
                     label_groups_by_cluster = FALSE)
p_neurons_vso

p_fetal_Q_vso<-plot_cells(cds_fetal_Q_vso, 
                          color_cells_by = "cell_type2", 
                          cell_size = 0.8,
                          label_principal_points = TRUE,
                          label_groups_by_cluster = FALSE)
p_fetal_Q_vso



cds_neurons_vso <- order_cells(cds_neurons_vso, reduction_method = 'UMAP') 
cds_fetal_Q_vso <- order_cells(cds_fetal_Q_vso, reduction_method = 'UMAP')
cds_repl_vso <- order_cells(cds_repl_vso, reduction_method = 'UMAP')
cds_astro_vso <- order_cells(cds_astro_vso, reduction_method = 'UMAP')


cds_neurons_COs <- order_cells(cds_neurons_COs, reduction_method = 'UMAP') # Not to use anyway; too few cells
cds_fetal_Q_COs <- order_cells(cds_fetal_Q_COs, reduction_method = 'UMAP')
cds_repl_COs <- order_cells(cds_repl_COs, reduction_method = 'UMAP')
cds_astro_COs <- order_cells(cds_astro_COs, reduction_method = 'UMAP')


# This will guide your starting point for hOL:

p_oligo_OPC_hOL<-plot_cells(cds_oligo_OPC_hOL, 
                            color_cells_by = "cell_type2", 
                            cell_size = 0.8,
                            label_principal_points = TRUE,
                            label_groups_by_cluster = FALSE)
p_oligo_OPC_hOL

cds_oligo_OPC_hOL <- order_cells(cds_oligo_OPC_hOL, reduction_method = 'UMAP') # Select starting point from graphical user interface


p1<-plot_cells(cds_oligo_OPC_hOL,
           color_cells_by = "pseudotime",
           label_cell_groups=FALSE,
           label_leaves=FALSE,
           label_branch_points=FALSE,
           cell_size=0.8,cell_stroke=0.2,
           graph_label_size=3) + ggtitle("OL OPC hOL")

p1

# Select trajectories for visualization and calculation of Moran's I statistics

p_repl_vso<-plot_cells(cds_repl_vso,
                       color_cells_by = "pseudotime",
                       group_cells_by = "day",
                       label_cell_groups=FALSE,
                       label_leaves=FALSE,
                       label_branch_points=FALSE,
                       cell_size=0.5,cell_stroke=0.2,
                       graph_label_size=3) + ggtitle("Fetal replicating VSO")
p_repl_vso

table(colData(cds_hOLs)$cell_type2)


p_OL_OPC_hOL<-plot_cells(cds_oligo_OPC_hOL,
                       color_cells_by = "pseudotime",
                       label_cell_groups=FALSE,
                       label_leaves=FALSE,
                       label_branch_points=FALSE,
                       cell_size=0.8,cell_stroke=0.4,
                       graph_label_size=3) + ggtitle("OPC and Oligodendrocytes hOL")
p_OL_OPC_hOL


p_fetal_Q_COs<-plot_cells(cds_fetal_Q_COs,
                      color_cells_by = "pseudotime",
                      label_cell_groups=FALSE,
                      label_leaves=FALSE,
                      label_branch_points=FALSE,
                      cell_size=0.8,cell_stroke=0.2,
                      graph_label_size=3) + ggtitle("Fetal quiescent COs")
p_fetal_Q_COs

p_fetal_Q_VSO<-plot_cells(cds_fetal_Q_vso,
                          color_cells_by = "pseudotime",
                          label_cell_groups=FALSE,
                          label_leaves=FALSE,
                          label_branch_points=FALSE,
                          cell_size=0.7,cell_stroke=0.2,
                          graph_label_size=3) + ggtitle("Fetal quiescent VSO")
p_fetal_Q_VSO

p_neurons_VSO<-plot_cells(cds_neurons_vso,
                          color_cells_by = "pseudotime",
                          label_cell_groups=FALSE,
                          label_leaves=FALSE,
                          label_branch_points=FALSE,
                          cell_size=0.8,cell_stroke=0.2,
                          graph_label_size=3) + ggtitle("Neurons VSO")

p_neurons_VSO # We will decide

p_neurons_COs<-plot_cells(cds_neurons_COs,
                          color_cells_by = "pseudotime",
                          label_cell_groups=FALSE,
                          label_leaves=FALSE,
                          label_branch_points=FALSE,
                          cell_size=0.8,cell_stroke=0.2,
                          graph_label_size=3) + ggtitle("Neurons COs")

p_neurons_COs # No, too few cells



# 3. Run Moran's I (knn)

res_neurons_vso <- graph_test(cds_neurons_vso, neighbor_graph = "knn", cores = 8)
res_fetal_Q_vso <- graph_test(cds_fetal_Q_vso, neighbor_graph = "knn", cores = 8)
res_repl_vso <- graph_test(cds_repl_vso, neighbor_graph = "knn", cores = 8)
res_astro_vso <- graph_test(cds_astro_vso, neighbor_graph = "knn", cores = 8)

res_fetal_Q_COs <- graph_test(cds_fetal_Q_COs, neighbor_graph = "knn", cores = 8)
res_repl_COs <- graph_test(cds_repl_COs, neighbor_graph = "knn", cores = 8)
res_astro_COs <- graph_test(cds_astro_COs, neighbor_graph = "knn", cores = 8)

res_oligo_OPC_hOL <- graph_test(cds_oligo_OPC_hOL, neighbor_graph = "knn", cores = 8)


print(dplyr::add_rownames(res_oligo_OPC_hOL) %>%
        dplyr::arrange(plyr::desc(morans_test_statistic), plyr::desc(-q_value)) %>% head(200),n=200)


# Define our slice thresholds; we use the outliers by IQR

neurons_vso_out<-boxplot(res_neurons_vso$morans_I)$out
neurons_vso_out<-which(res_neurons_vso$morans_I %in% neurons_vso_out)
neurons_vso_out<-res_neurons_vso[neurons_vso_out,]
neurons_vso_out<-neurons_vso_out$gene_name

fetal_Q_vso_out<-boxplot(res_fetal_Q_vso$morans_I)$out
fetal_Q_vso_out<-which(res_fetal_Q_vso$morans_I %in% fetal_Q_vso_out)
fetal_Q_vso_out<-res_fetal_Q_vso[fetal_Q_vso_out,]
fetal_Q_vso_out<-fetal_Q_vso_out$gene_name

repl_vso_out<-boxplot(res_repl_vso$morans_I)$out
repl_vso_out<-which(res_repl_vso$morans_I %in% repl_vso_out)
repl_vso_out<-res_repl_vso[repl_vso_out,]
repl_vso_out<-repl_vso_out$gene_name

astro_vso_out<-boxplot(res_astro_vso$morans_I)$out
astro_vso_out<-which(res_astro_vso$morans_I %in% astro_vso_out)
astro_vso_out<-res_astro_vso[astro_vso_out,]
astro_vso_out<-astro_vso_out$gene_name

fetal_Q_COs_out<-boxplot(res_fetal_Q_COs$morans_I)$out
fetal_Q_COs_out<-which(res_fetal_Q_COs$morans_I %in% fetal_Q_COs_out)
fetal_Q_COs_out<-res_fetal_Q_COs[fetal_Q_COs_out,]
fetal_Q_COs_out<-fetal_Q_COs_out$gene_name

astro_COs_out<-boxplot(res_astro_COs$morans_I)$out
astro_COs_out<-which(res_astro_COs$morans_I %in% astro_COs_out)
astro_COs_out<-res_astro_COs[astro_COs_out,]
astro_COs_out<-astro_COs_out$gene_name

repl_COs_out<-boxplot(res_repl_COs$morans_I)$out
repl_COs_out<-which(res_repl_COs$morans_I %in% repl_COs_out)
repl_COs_out<-res_repl_COs[repl_COs_out,]
repl_COs_out<-repl_COs_out$gene_name


OL_OPC_hOL_out<-boxplot(res_oligo_OPC_hOL$morans_I)$out
OL_OPC_hOL_out<-which(res_oligo_OPC_hOL$morans_I %in% OL_OPC_hOL_out)
OL_OPC_hOL_out<-res_oligo_OPC_hOL[OL_OPC_hOL_out,]
OL_OPC_hOL_out<-OL_OPC_hOL_out$gene_name
OL_OPC_hOL_out

# Extract lists of interest:

Gene_sets_from_trajectory_outliers_COs<-list("Fetal Q COs"=fetal_Q_COs_out,
                                       "Replicating COs"=repl_COs_out,
                                       "Astro COs"=astro_COs_out)


Gene_sets_from_trajectory_outliers_vso<-list("Neurons vso"=neurons_vso_out,
                                       "Fetal Q vso"=fetal_Q_vso_out,
                                       "Replicating vso"=repl_vso_out,
                                       "Astro vso"=astro_vso_out)


Gene_sets_from_trajectory_outliers_hOL<-OL_OPC_hOL_out

Gene_sets_from_trajectory_outliers_hippo<-list("Neurons hippo"=neurons_hippo_out,
                                             "Fetal Q hippo"=fetal_Q_hippo_out,
                                             "Replicating hippo"=repl_hippo_out)


save(Gene_sets_from_trajectory_outliers_COs,Gene_sets_from_trajectory_outliers_hOL,
     Gene_sets_from_trajectory_outliers_vso,
     Gene_sets_from_trajectory_outliers_hippo,
     file="Gene_sets_from_trajectory_outliers_COs_vso_hOL_hippo_final_01102026.RData")

# Use these sets for gene set enrichment analysis with GeneOverlap


##### Compare cluster for COs, VSO and hOLs gene sets and 01312026 for Fig.5 #######

# Load the sets of bulk RNA-Seq DCG
# Create a character vector from all the genes expressed across the four sets of organoids ("Background_genes")
# Use the gene sets created above: "Gene_sets_from_trajectory_outliers_COs_vso_hOL_hippo_final_01102026.RData"

library(clusterProfiler)
library(readxl)

Universe<-Background_genes

library(org.Hs.eg.db)

Organoids_genes_bulk_out<-list("hOL"=Gene_sets_from_trajectory_outliers_hOL,
                               "Fetal COs"=Gene_sets_from_trajectory_outliers_COs$`Fetal Q COs`,
                               "Repl COs"=Gene_sets_from_trajectory_outliers_COs$`Replicating COs`,
                               "Astro COs"=Gene_sets_from_trajectory_outliers_COs$`Astro COs`,
                               "Neurons hippo"=Gene_sets_from_trajectory_outliers_hippo$`Neurons hippo`,
                               "Fetal Q hippo"=Gene_sets_from_trajectory_outliers_hippo$`Fetal Q hippo`,
                               "Repl hippo"=Gene_sets_from_trajectory_outliers_hippo$`Replicating hippo`,
                               "Neurons VSO"=Gene_sets_from_trajectory_outliers_vso$`Neurons vso`,
                               "Fetal Q VSO"=Gene_sets_from_trajectory_outliers_vso$`Fetal Q vso`,
                               "Repl Q VSO"=Gene_sets_from_trajectory_outliers_vso$`Replicating vso`,
                               "Astro VSO"=Gene_sets_from_trajectory_outliers_vso$`Astro vso`)

names(Organoids_genes_bulk_out)

mydf<-Organoids_genes_bulk_out

names(mydf)
group_names <-names(mydf)
group_sizes <- lengths(mydf)

mydf<-as.data.frame(unlist(mydf))

group_names
group_sizes

Group <- rep(group_names, group_sizes)

# Check the result
print(table(Group))

mydf$Group<-Group
names(mydf)<-c("Symbol","Group")

# Compare cluster:
library(clusterProfiler)

Universe=Background_genes
formula_res <- compareCluster(Symbol~Group, data=mydf, fun="enrichGO",OrgDb = org.Hs.eg.db,
                              keyType = "SYMBOL",ont = "BP",
                              pvalueCutoff = 0.001,
                              minGSSize    = 10,
                              maxGSSize    = 500,
                              pAdjustMethod = "bonferroni",
                              universe=Universe,
                              qvalueCutoff = 0.001)


formula_test<-formula_res

formula_final<-clusterProfiler::simplify(formula_test,cutoff = 0.7,
                                         by = "p.adjust",
                                         select_fun = min,
                                         measure = "Wang",
                                         semData = NULL)

pp <- enrichplot::dotplot(formula_final, x="Modules",showCategory = 5,label_format = 120) + facet_grid(. ~ Group, labeller = label_wrap_gen(width = 10)) +
  aes(x=as.factor(Group)) + xlab("Modules") +
  scale_fill_gradient(low="red",high="grey80",
                      guide=guide_colorbar(reverse=TRUE, order=1)) +
  guides(size = guide_legend(override.aes=list(shape=1))) +
  theme(panel.grid.major.y = element_line(linetype='dotted', color='#808080'),
        panel.grid.major.x = element_blank())


pp + theme(axis.text.x = element_text(colour="black",face="bold",size=7,angle = 60,hjust=1),
           axis.text.y = element_text(colour="black",face="bold",size=8),
           strip.text = element_text(size = 8,face="bold",colour = "darkred"))



##### rrvgo for Semantic Similarities Between Cell Types organoids #####

# This section assumes `formula_final` object is available from the enrichment analysis.

if (exists("formula_final") && inherits(formula_final, "compareClusterResult")) {
  message("Calculating semantic similarity and reducing terms with rrvgo...")
  
  simMatrix <- calculateSimMatrix(
    formula_final@compareClusterResult$ID,
    orgdb = "org.Hs.eg.db",
    ont = "BP",
    method = "Wang"
  )
  
  scores <- setNames(-log10(formula_final@compareClusterResult$qvalue), formula_final@compareClusterResult$ID)
  
  reducedTerms <- reduceSimMatrix(
    simMatrix,
    scores = scores,
    threshold = 0.9,
    orgdb = "org.Hs.eg.db"
  )
  
  # Plotting with rrvgo
  
heatmapPlot(simMatrix, reducedTerms, annotateParent = TRUE, annotationLabel = "parentTerm", fontsize = 6) %>% print()
treemapPlot(reducedTerms) %>% print()
scatterPlot(simMatrix, reducedTerms) %>% print()

# Create the specific scatter-plot from figure 5:
# Store results for different cell type groups if you run this iteratively
# Example for inhibitory, you would save it specific to inhibitory
  
simMatrix_Inhib <- simMatrix
reducedTerms_Inhib <- reducedTerms
  
  # Assuming you would run this for Glia and Excitatory as well, then save them all
  # save(Glia_genes, Excit_genes, Inhib_genes, simMatrix_Excit, simMatrix_Glia, simMatrix_Inhib,
  #      reducedTerms_Excit, reducedTerms_Glia, reducedTerms_Inhib,
  #      file = here(base_dir, "For_rrvgo_cell_groups_glia_excit_inhib_", format(Sys.Date(), "%m%d%Y"), ".RData"))
} else {
  message("`formula_final` object not found or is not a 'compareClusterResult'. Skipping rrvgo section.")
}

collectGarbage()

# Step 1: Calculate the x and y coordinates from the similarity matrix

mds <- cmdscale(as.dist(1 - simMatrix), eig = TRUE, x.ret = TRUE)
coords <- as.data.frame(mds$points)
colnames(coords) <- c("x", "y")
coords$go <- rownames(coords)

# Step 2: Add the x and y coordinates to your reducedTerms data frame
reducedTerms <- merge(reducedTerms, coords, by = "go", all.x = TRUE)

# Step 3: Add the parent p.adjust values from the original enrichment object
p_adjust_map <- setNames(formula_final@compareClusterResult$p.adjust, formula_final@compareClusterResult$ID)
reducedTerms$parent_p.adjust <- p_adjust_map[reducedTerms$parent]

# Assume 'reducedTerms' dataframe has already been created from
# rrvgo::reduceSimMatrix and has 'x', 'y', and 'parent_p.adjust' columns added.

# Step 1: Identify the unique parent GO IDs
unique_parent_ids <- reducedTerms %>%
  distinct(parent) %>%
  pull(parent)

# Step 2: Filter reducedTerms to keep only the rows that correspond to these unique parent IDs
# We will use the 'go' column to do this, as 'go' is the unique ID for each row.
# We also need to get the term name for each parent GO ID

parent_terms_to_label <- reducedTerms %>%
  filter(go %in% unique_parent_ids) %>%
  arrange(parent_p.adjust) %>%
  slice_head(n = 15)

library(ggplot2)
library(ggrepel)
library(dplyr)

# Assuming 'reducedTerms' contains all terms, and 'parent_terms_to_label'
# contains the top unique parent terms with their coordinates and p.adjust values.

p<-ggplot(data = reducedTerms, aes(x = x, y = y))

p +
  # Layer 1: Plot all terms as small, light grey dots to give context.
  geom_point(data = reducedTerms, aes(x = x, y = y),
             color = "grey60", size = 3, alpha = 0.4) + # size 2 for organoids
  # Layer 2: Plot the unique parent terms on top, mapping fill and size to p.adjust.
  geom_point(data = parent_terms_to_label,
             aes(x = x, y = y, size = parent_p.adjust, fill = parent_p.adjust),
             shape = 21, alpha = 0.5, stroke = 1) +
  # Layer 3: Add labels using ggrepel.
  geom_text_repel(data = parent_terms_to_label,
                  aes(x = x, y = y, label = parentTerm),
                  size = 5.5,
                  color = "black",
                  box.padding = unit(1.5, "lines"), # 0.4 organoids
                  # point.padding = unit(0.1, "lines"),# 0.5 organoids
                  force = 6, # no force for organoids
                  segment.color = 'black',
                  segment.size = 0.4, # 0.5 for organoids
                  min.segment.length = 0, # 0 for organoids
                  max.overlaps =Inf)   +            # max.overlaps = Inf) + for organoids
  # Define the size range for the parent term bubbles.
  scale_size(range = c(5, 12)) +
  # Define the fill color scale using a viridis palette.
  # scale_fill_viridis_c(name = "Fill based on\np-adjust", direction = -1) +
  scale_fill_distiller(palette = "Greens", name = "p-adjust", direction = 1) +  # "Blues" for organoids
  # Use a minimalist theme to remove clutter.
  theme_minimal() +
  theme(
    plot.title = element_text(hjust = 0.3, face = "bold", margin = margin(b = 15)),
    axis.title = element_blank(),
    panel.grid.major = element_blank(),
    panel.grid.minor = element_blank(),
    legend.position = c(0.95, 0.05),
    legend.justification = c("right", "bottom"),
    aspect.ratio = 1 # 1.2 organoids
  ) +
  labs(title = "GO:BP of genes from modules with DME") +
  # This is the key line to remove the size legend!
  guides(size = "none")

